{
  "type": "object",
  "required": ["productIndex"],
  "properties": {
    "productIndex": {
      "type": "integer",
      "description": "The index of the product in the list that the user wants to open"
    }
  }
}
